# Homebrew Tap for Speqs

## Install

```bash
brew tap speqs-io/tap
brew install speqs
```

## About

This tap contains the Homebrew formula for the [Speqs CLI](https://speqs.io).

The formula is automatically updated when new versions are published to npm.
